<div class="bootstrap-wrapper">

  <p>{{ searchForEventsBetween }}</p>

  <form action="" method="get">

    <p>{{ from }} :</p>
    <p><input id="from" class="datepickerFrom" name="from" type="text"></p>
    <p></p>
    <p>{{ to }} :</p>
    <p><input id="to" class="datepickerTo" name="to" type="text"></p>
    <p></p>
    <p>{{ bookableItems }} :</p>
    <!-- TODO:bookable items dropdownlist-->
    <select class="form-control">
      <option>1</option>
      <option>2</option>
      <option>3</option>
      <option>4</option>
      <option>5</option>
    </select>

    <p>{{ location }} :</p>
    <!-- TODO:location txt-->
    <p>{{ description }} :</p>
    <!-- TODO:description txt-->


    <input type="submit" name="submit" value="{{ search }}">
  </form>

  <div id="container"></div>

  <!-- TODO : Check why this made a TypeError: undefined is not an object (evaluating 'jQuery.fn.pickatime.defaults') -->
  {% if pickerTranslated|length > 0 %}
  <script type="text/javascript">
  {{pickerTranslated|raw }}
  </script>
  {% endif %}


  <script type="text/javascript">
  var $input = jQuery('.datepickerFrom').pickadate({
    format: '{{dateFormat}}',
    // min: [2015, 7, 14],
    container: '#container',
    // editable: true,
    closeOnSelect: false,
    closeOnClear: false,
  })
  var picker = $input.pickadate('picker')
  // picker.set('select', '14 October, 2014')
  // picker.open()

  // $('button').on('click', function() {
  //     picker.set('disable', true);
  // });

  </script>

  <script type="text/javascript">
  var $input = jQuery('.datepickerTo').pickadate({
    format: '{{dateFormat}}',
    container: '#container',
    closeOnSelect: false,
    closeOnClear: false,
  })
  var picker = $input.pickadate('picker')
  </script>

  <p><a href="http://sagenda.net/Accounts/Register">{{ createAFreeBookingAccount }} </a></p>

  {% block searchResult %}

{% endblock %}
<!-- TODO:Booking list-->

<!-- TODO:pagination-->
</div>
